---
nav_group: "Candlestick"
nav_order: 1
title: "Default"
description: "A candlestick chart showing stock price OHLC data"
image: ""
files:
  App.js: |
    import Widget from "@meursyphus/flitter-react";
    import { CandlestickChart } from "@meursyphus/headless-chart";
    import {
      Text,
      Container,
      EdgeInsets,
      TextStyle,
      Column,
      Row,
      SizedBox,
      MainAxisAlignment,
      MainAxisSize,
      BoxDecoration,
      Padding,
      Center,
    } from "@meursyphus/flitter";

    const data = {
      labels: ["Mon", "Tue", "Wed", "Thu", "Fri", "Mon", "Tue", "Wed", "Thu", "Fri"],
      datasets: [
        { open: 150, high: 158, low: 148, close: 155 },
        { open: 155, high: 162, low: 153, close: 160 },
        { open: 160, high: 165, low: 155, close: 157 },
        { open: 157, high: 163, low: 152, close: 161 },
        { open: 161, high: 168, low: 159, close: 166 },
        { open: 166, high: 170, low: 160, close: 162 },
        { open: 162, high: 167, low: 158, close: 164 },
        { open: 164, high: 172, low: 163, close: 170 },
        { open: 170, high: 175, low: 165, close: 168 },
        { open: 168, high: 176, low: 166, close: 174 },
      ],
      title: "ACME Corp Stock Price",
    };

    const chart = CandlestickChart({
      data,
      custom: {
        layout: ({ title, legends, plot }) =>
          Container({
            padding: EdgeInsets.only({ left: 50, bottom: 50, right: 20, top: 30 }),
            child: Column({
              children: [
                Center({
                  child: Text("ACME Corp Stock Price (2 Weeks)", {
                    style: new TextStyle({
                      fontSize: 16,
                      fontWeight: "bold",
                      color: "#333333",
                      fontFamily: "Noto Sans JP",
                    }),
                  }),
                }),
                SizedBox({ height: 16 }),
                plot,
              ],
            }),
          }),
        xAxisLabel: ({ name }) =>
          Padding({
            padding: EdgeInsets.only({ top: 4 }),
            child: Text(name, {
              style: new TextStyle({
                fontSize: 11,
                color: "#666666",
                fontFamily: "Noto Sans JP",
              }),
            }),
          }),
        yAxisLabel: ({ name }) =>
          Padding({
            padding: EdgeInsets.only({ right: 4 }),
            child: Text(name, {
              style: new TextStyle({
                fontSize: 11,
                color: "#666666",
                fontFamily: "Noto Sans JP",
              }),
            }),
          }),
        xAxisTick: () => Container({ height: 6, width: 1, color: "#DDDDDD" }),
        yAxisTick: () => Container({ height: 1, width: 6, color: "#DDDDDD" }),
        xAxisLine: () => Container({ color: "#BBBBBB", height: 1, width: Infinity }),
        yAxisLine: () => Container({ color: "#BBBBBB", width: 1, height: Infinity }),
        gridXLine: () => Container({ height: 1, color: "#F0F0F0" }),
        gridYLine: () => Container({ width: 1, color: "#F0F0F0" }),
      },
    });

    export default function App() {
      return (
        <Widget
          width="auto"
          height="450px"
          widget={chart}
          renderer="svg"
        />
      );
    }
---

# Candlestick Chart Example

A candlestick chart displaying two weeks of OHLC (Open, High, Low, Close) stock price data for a fictional company, with green candles for bullish days and red for bearish.
